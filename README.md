# Steps to use

1. Create virtual environment
   1. Run: `python -m venv .venv`
   2. Activate: `.venv\Scripts\activate`

2. Install important libraries
   1. Run: `pip install -r requirements.txt`

3. Run backend
   1. Run: `python .\v1\api\generated_content.py`
